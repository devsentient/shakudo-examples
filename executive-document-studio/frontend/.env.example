# For the single-service deployment, the built frontend talks to FastAPI on the same origin.
NEXT_PUBLIC_API_BASE_URL=/api

# If you run the frontend separately during development, you can override it, e.g.:
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8787/api
